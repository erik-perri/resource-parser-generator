import {null, object, output, string, union} from 'zod';

export const userResourceAdminListParser = object({
  id: string(),
  email: string(),
  name: string(),
  created_at: union(null(), string()),
  updated_at: union(null(), string()),
});

export type UserResourceAdminList = output<typeof userResourceAdminListParser>;
